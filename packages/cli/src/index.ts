// Placeholder for future CLI migration.
// The canonical CLI implementation currently lives at src/cli.ts and is
// bundled into dist/cli.js by the root esbuild step.
export const CLI_PACKAGE_RESERVED = true;
