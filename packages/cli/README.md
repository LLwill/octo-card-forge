# CLI migration target

The published `@mlt-org/octo-card-cli` package remains at the repository root during Phase 1.
Phase 3 moves that package into this directory without changing its npm name or `octo-card` bin.

